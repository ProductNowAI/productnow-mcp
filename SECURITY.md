# Security

Report security concerns to `security@productnow.ai`.

Do not open public issues for suspected vulnerabilities, exposed credentials, or
customer data concerns.

This repository only contains public MCP metadata and documentation. The hosted
MCP server itself requires OAuth authentication and applies ProductNow resource
permissions to every request.

